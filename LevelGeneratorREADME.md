关卡生成器

由指定音乐，通过libroasa库来分析节拍信息，得到BPM(每一拍对应180°)。

节拍之间的间隔正比于角度，但注意要每个间隔都只能是30°的整数倍，因此需要识别出实际上不是节拍的噪音数据并剔除，并进行节拍长度量子化，确保关卡能踩上点。

用PyQt6做出GUI界面，支持在/audio中选择音乐，生成关卡文件到/levels。

玩家可以自行决定随机加入reverse事件的比例（默认为0）。

同时有测试按钮可以立即玩一个关卡来测试。